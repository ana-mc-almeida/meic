

> Every pool has always to have an start and a end events

> Sequence flow **cannot** cross pool boundaries. Message flow **can** cross pool boundaries.
> Both sequence and message flows **can** cross lane boundaries.
> Message flow **cannot connect** two flow elements within the same pool.
